diff --git a/packages/core/src/components/ExpandableContent/ExpandableContent.stories.tsx b/packages/core/src/components/ExpandableContent/ExpandableContent.stories.tsx
deleted file mode 100644
index a20fef0..0000000
--- a/packages/core/src/components/ExpandableContent/ExpandableContent.stories.tsx
+++ /dev/null
@@ -1,68 +0,0 @@
-// packages/core/src/components/ExpandableContent/ExpandableContent.stories.tsx
-import type { Meta, StoryObj } from '@storybook/react';
-import * as React from 'react';
-import { ExpandableContent } from './ExpandableContent';
-
-const SAMPLE_TEXT =
-  'Lorem ipsum dolor sit amet, consectetur adipiscing elit. Donec at felis a orci efficitur gravida. '; // repeated to exceed clamp
-
-const LONG_TEXT = `${SAMPLE_TEXT.repeat(6)}Vestibulum ante ipsum primis in faucibus orci luctus et ultrices posuere cubilia curae.`;
-
-const meta: Meta<typeof ExpandableContent> = {
-  title: 'Components/ExpandableContent',
-  component: ExpandableContent,
-  argTypes: {
-    onExpandedChange: { action: 'expanded change' }
-  }
-};
-
-export default meta;
-
-type Story = StoryObj<typeof ExpandableContent>;
-
-export const Default: Story = {
-  args: {
-    defaultExpanded: false,
-    children: SAMPLE_TEXT.repeat(3)
-  }
-};
-
-export const WithDefaultExpanded: Story = {
-  args: {
-    defaultExpanded: true,
-    children: SAMPLE_TEXT.repeat(3)
-  }
-};
-
-export const Controlled: Story = {
-  args: {
-    expanded: false,
-    children: SAMPLE_TEXT.repeat(3)
-  },
-  render: args => {
-    const [expanded, setExpanded] = React.useState(args.expanded ?? false);
-    React.useEffect(() => {
-      setExpanded(args.expanded ?? false);
-    }, [args.expanded]);
-
-    return (
-      <ExpandableContent
-        {...args}
-        expanded={expanded}
-        onExpandedChange={next => {
-          setExpanded(next);
-          args.onExpandedChange?.(next);
-        }}
-      >
-        {args.children}
-      </ExpandableContent>
-    );
-  }
-};
-
-export const LongContent: Story = {
-  args: {
-    defaultExpanded: false,
-    children: LONG_TEXT
-  }
-};
diff --git a/packages/core/src/components/ExpandableContent/ExpandableContent.test.tsx b/packages/core/src/components/ExpandableContent/ExpandableContent.test.tsx
deleted file mode 100644
index a966410..0000000
--- a/packages/core/src/components/ExpandableContent/ExpandableContent.test.tsx
+++ /dev/null
@@ -1,120 +0,0 @@
-import '@testing-library/jest-dom';
-// This file was generated by an AI agent. Do not edit directly.
-import * as React from 'react';
-import { render, screen } from '@testing-library/react';
-import userEvent from '@testing-library/user-event';
-import { ExpandableContent, type ExpandableContentProps } from './ExpandableContent';
-
-const DEFAULT_TEXT = 'Curabitur vitae nisl vitae nibh elementum tincidunt.';
-
-const renderExpandableContent = (props?: Partial<ExpandableContentProps>) => {
-  const { container } = render(
-    <ExpandableContent {...props}>
-      {props?.children ?? DEFAULT_TEXT.repeat(3)}
-    </ExpandableContent>
-  );
-
-  const getControl = () =>
-    screen.getByRole('button', { name: 'Toggle expanded content' }) as HTMLButtonElement;
-
-  const getContent = () => {
-    const control = getControl();
-    const controlsId = control.getAttribute('aria-controls');
-    return (controlsId ? document.getElementById(controlsId) : null) as HTMLElement | null;
-  };
-
-  return { getControl, getContent, container };
-};
-
-describe('ExpandableContent', () => {
-  it('renders content slot', () => {
-    renderExpandableContent();
-    expect(screen.getByText(DEFAULT_TEXT, { exact: false })).toBeInTheDocument();
-  });
-
-  it('renders control button', () => {
-    renderExpandableContent();
-    expect(screen.getByRole('button', { name: 'Toggle expanded content' })).toBeInTheDocument();
-  });
-
-  describe('uncontrolled toggle', () => {
-    it('sets aria-expanded to true after click', async () => {
-      const { getControl } = renderExpandableContent();
-      const control = getControl();
-      const user = userEvent.setup();
-      await user.click(control);
-      expect(control).toHaveAttribute('aria-expanded', 'true');
-    });
-
-    it('updates content data-state to open after click', async () => {
-      const { getControl, getContent } = renderExpandableContent();
-      const control = getControl();
-      const user = userEvent.setup();
-      await user.click(control);
-      const content = getContent();
-      expect(content?.getAttribute('data-state')).toBe('open');
-    });
-  });
-
-  describe('controlled mode', () => {
-    it('calls onExpandedChange with next state', async () => {
-      const handleExpandedChange = jest.fn();
-      const { getControl } = renderExpandableContent({ expanded: false, onExpandedChange: handleExpandedChange });
-      const control = getControl();
-      const user = userEvent.setup();
-      await user.click(control);
-      expect(handleExpandedChange).toHaveBeenCalledWith(true);
-    });
-
-    it('does not change aria-expanded without external state update', async () => {
-      const { getControl } = renderExpandableContent({ expanded: false, onExpandedChange: () => {} });
-      const control = getControl();
-      const user = userEvent.setup();
-      await user.click(control);
-      expect(control).toHaveAttribute('aria-expanded', 'false');
-    });
-  });
-
-  it('links aria-controls to the content region id', () => {
-    const { getControl, getContent } = renderExpandableContent();
-    const control = getControl();
-    const content = getContent();
-    expect(control.getAttribute('aria-controls')).toBe(content?.id ?? null);
-  });
-
-  it('applies closed-state clamping classes', () => {
-    const { getContent } = renderExpandableContent();
-    const content = getContent();
-    const hasClosedClasses = Boolean(
-      content?.classList.contains('data-[state=closed]:mh-75') &&
-        content?.classList.contains('data-[state=closed]:line-clamp-3')
-    );
-    expect(hasClosedClasses).toBe(true);
-  });
-
-  it('applies rotate-0 class to control in closed state', () => {
-    const { getControl } = renderExpandableContent();
-    const control = getControl();
-    expect(control.classList.contains('peer-data-[state=closed]:rotate-0')).toBe(true);
-  });
-
-  it('omits closed-state classes when expanded', () => {
-    const { getContent } = renderExpandableContent({ defaultExpanded: true });
-    const content = getContent();
-    expect(content?.classList.contains('data-[state=closed]:mh-75')).toBe(false);
-  });
-
-  it('applies rotate-180 class to control when expanded', () => {
-    const { getControl } = renderExpandableContent({ defaultExpanded: true });
-    const control = getControl();
-    expect(control.classList.contains('peer-data-[state=open]:rotate-180')).toBe(true);
-  });
-
-  it('renders content before control in the DOM', () => {
-    const { container, getContent } = renderExpandableContent();
-    const root = container.firstElementChild as HTMLElement | null;
-    const content = getContent();
-    const isContentFirst = Boolean(root?.firstElementChild === content);
-    expect(isContentFirst).toBe(true);
-  });
-});
diff --git a/packages/core/src/components/ExpandableContent/ExpandableContent.tsx b/packages/core/src/components/ExpandableContent/ExpandableContent.tsx
deleted file mode 100644
index f201f53..0000000
--- a/packages/core/src/components/ExpandableContent/ExpandableContent.tsx
+++ /dev/null
@@ -1,75 +0,0 @@
-// packages/core/src/components/ExpandableContent/ExpandableContent.tsx
-import * as React from 'react';
-import { IconButton } from '../IconButton';
-import { ChevronDownIcon } from '@radix-ui/react-icons';
-import { composeClasses } from '../../utilities/composeClasses/composeClasses';
-
-const CONTENT_BASE_CLASSES =
-  'peer overflow-hidden transition-[max-height] duration-200 ease-in-out';
-const CONTENT_CLOSED_CLASSES = 'data-[state=closed]:mh-75 data-[state=closed]:line-clamp-3';
-const CONTROL_CLASSES =
-  'text-center transition-transform peer-data-[state=open]:rotate-180 peer-data-[state=closed]:rotate-0';
-
-export interface ExpandableContentProps
-  extends Omit<React.HTMLAttributes<HTMLDivElement>, 'children'> {
-  defaultExpanded?: boolean;
-  expanded?: boolean;
-  onExpandedChange?: (expanded: boolean) => void;
-  children?: React.ReactNode;
-}
-
-export const ExpandableContent = React.forwardRef<HTMLDivElement, ExpandableContentProps>(
-  (
-    {
-      defaultExpanded = false,
-      expanded: expandedProp,
-      onExpandedChange,
-      children,
-      className,
-      ...rest
-    },
-    ref
-  ) => {
-    const contentId = React.useId();
-    const isControlled = expandedProp !== undefined;
-    const [uncontrolledExpanded, setUncontrolledExpanded] = React.useState(defaultExpanded);
-    const expanded = isControlled ? Boolean(expandedProp) : uncontrolledExpanded;
-
-    const handleToggle = React.useCallback(() => {
-      const nextExpanded = !expanded;
-      if (!isControlled) {
-        setUncontrolledExpanded(nextExpanded);
-      }
-      onExpandedChange?.(nextExpanded);
-    }, [expanded, isControlled, onExpandedChange]);
-
-    const contentClasses = composeClasses(
-      CONTENT_BASE_CLASSES,
-      expanded ? undefined : CONTENT_CLOSED_CLASSES
-    );
-
-    const controlClasses = composeClasses(CONTROL_CLASSES);
-
-    return (
-      <div ref={ref} className={className} {...rest}>
-        <div
-          id={contentId}
-          data-state={expanded ? 'open' : 'closed'}
-          className={contentClasses}
-        >
-          {children}
-        </div>
-        <IconButton
-          aria-expanded={expanded}
-          aria-controls={contentId}
-          onClick={handleToggle}
-          ariaLabel="Toggle expanded content"
-          icon={<ChevronDownIcon />}
-          className={controlClasses}
-        />
-      </div>
-    );
-  }
-);
-
-ExpandableContent.displayName = 'ExpandableContent';
diff --git a/packages/core/src/components/ExpandableContent/index.ts b/packages/core/src/components/ExpandableContent/index.ts
deleted file mode 100644
index 929759b..0000000
--- a/packages/core/src/components/ExpandableContent/index.ts
+++ /dev/null
@@ -1 +0,0 @@
-export * from './ExpandableContent';
