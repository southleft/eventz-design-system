{
  "name": "doxyz-monorepo",
  "private": true,
  "packageManager": "pnpm@9.12.3",
  "scripts": {
    "build": "pnpm --filter @doxyz-ui/core build",
    "build:tokens": "pnpm --filter @doxyz-ui/core build:tokens",
    "build-storybook": "pnpm --filter @doxyz-ui/core build-storybook",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "type-check": "tsc -b --noEmit",
    "sb": "pnpm --filter @doxyz-ui/core storybook",
    "sb:build": "pnpm --filter @doxyz-ui/core build:storybook",
    "pack:ui": "pnpm --filter @doxyz-ui/core exec npm pack --dry-run",
    "agents:sync": "node scripts/agents-sync.mjs",
    "agents:verify": "node scripts/agents-sync.mjs && markdown-link-check -q AGENTS/*.md"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "jest": "^30.1.1",
    "js-yaml": "^4.1.0",
    "markdown-link-check": "^3.11.2",
    "ts-jest": "^29.4.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  }
}
