# Skill: Log Aging Check
Given a `.log` file path and today’s date, return `true` if the file is older than 30 days.

Inputs:
- filePath
- currentDate

Output:
- boolean (true if older than 30 days)
