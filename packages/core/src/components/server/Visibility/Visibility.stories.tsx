// packages/core/src/components/server/Visibility/Visibility.stories.tsx
// Generated by AI (DoXYZ agents). Do not edit directly.
// Summary: Stories demonstrating Visibility baseline render plus breakpoint-bound examples using from/to props.

import type { Meta, StoryObj } from '@storybook/react';
import * as React from 'react';
import { Visibility, type VisibilityProps } from './Visibility';

const demoContainerStyle: React.CSSProperties = {
  display: 'grid',
  gap: '1rem'
};

const demoBlockStyle: React.CSSProperties = {
  border: '1px dashed #94a3b8',
  borderRadius: '0.75rem',
  padding: '1rem'
};

const demoLabelStyle: React.CSSProperties = {
  fontWeight: 600,
  marginBottom: '0.25rem'
};

const demoDescriptionStyle: React.CSSProperties = {
  fontSize: '0.875rem',
  color: '#475569'
};

const meta: Meta<VisibilityProps> = {
  title: 'Server components/Visibility',
  component: Visibility,
  args: {
    children: 'Responsive content block'
  },
  parameters: {
    controls: {
      include: ['from', 'to']
    }
  }
};

export default meta;

type Story = StoryObj<VisibilityProps>;

export const Default: Story = {
  args: {}
};

const ExampleBlock = ({ label, description }: { label: string; description: string }) => (
  <div style={demoBlockStyle}>
    <div style={demoLabelStyle}>{label}</div>
    <div style={demoDescriptionStyle}>{description}</div>
  </div>
);

export const Examples: Story = {
  render: () => (
    <div style={demoContainerStyle}>
      <Visibility from="lg">
        <ExampleBlock label='from="lg"' description="Hidden until lg breakpoint." />
      </Visibility>
      <Visibility to="xl">
        <ExampleBlock label='to="xl"' description="Visible until xl breakpoint." />
      </Visibility>
      <Visibility from="md" to="xl">
        <ExampleBlock label='from="md" to="xl"' description="Visible only between md and xl." />
      </Visibility>
      <Visibility from="xl" to="md">
        <ExampleBlock label='from="xl" to="md"' description="Hidden strictly between md and xl." />
      </Visibility>
      <Visibility from="md" to="md">
        <ExampleBlock label='from="md" to="md"' description="Hidden everywhere (conflicting bounds)." />
      </Visibility>
    </div>
  )
};
