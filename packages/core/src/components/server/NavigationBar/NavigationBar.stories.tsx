// packages/core/src/components/server/NavigationBar/NavigationBar.stories.tsx
// Generated by AI (Eventz agents). Do not edit directly.
// Summary: Stories for NavigationBar demonstrating default nav, fixed positioning, mobile navigation slot, logo slot, and long item lists.

import * as React from 'react';
import type { Meta, StoryObj } from '@storybook/react';
import { Markdown } from '@storybook/addon-docs/blocks';
import { Button } from '../../client/Button';
import { NavigationDropdown } from '../../client/NavigationDropdown';
import { NavigationBar, NavigationBarProps } from './NavigationBar';
import Readme from './README.md?raw';

const baseItems: NavigationBarProps['items'] = [
  { label: 'Overview', href: '/overview' },
  { label: 'Solutions', href: '/solutions' },
  { label: 'Pricing', href: '/pricing' },
  { label: 'Docs', href: '/docs' }
];

const defaultLogo = <span className="text-lg font-bold text-color-content-default">Eventz</span>;

const meta: Meta<NavigationBarProps> = {
  title: 'Server components/NavigationBar',
  component: NavigationBar,
  tags: ['autodocs'],
  parameters: {
    docs: {
      page: () => <Markdown>{Readme}</Markdown>
    }
  },
  args: {
    ariaLabel: 'Primary navigation',
    items: baseItems,
    logo: defaultLogo
  }
};

export default meta;

type Story = StoryObj<NavigationBarProps>;

export const Default: Story = {
  args: {
    mobileNavigation: (
      <NavigationDropdown
        items={[
          { label: 'Overview', href: '/overview' },
          { label: 'Solutions', href: '/solutions' },
          { label: 'Pricing', href: '/pricing' }
        ]}
      />
    )
  }
};

export const Fixed: Story = {
  args: {
    fixed: true,
    mobileNavigation: (
      <NavigationDropdown
        items={[
          { label: 'Overview', href: '/overview' },
          { label: 'Solutions', href: '/solutions' },
          { label: 'Pricing', href: '/pricing' }
        ]}
      />
    )
  }
};

export const NoDropdown: Story = {};

export const RichSecondary: Story = {
  args: {
    mobileNavigation: (
      <NavigationDropdown
        items={[
          { label: 'Overview', href: '/overview' },
          { label: 'Solutions', href: '/solutions' },
          { label: 'Pricing', href: '/pricing' }
        ]}
      />
    ),
    secondaryNavigation: <Button>Sign in</Button>
  }
};

export const WithTagline: Story = {
  args: {
    items: undefined,
    tagline: (
      <div className="text-xs uppercase text-color-content-default">
        © 2025 DO312 All Rights Reserved
      </div>
    )
  }
};
