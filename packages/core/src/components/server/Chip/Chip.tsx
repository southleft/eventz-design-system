// packages/core/src/components/server/Chip/Chip.tsx
// Generated by AI (DoXYZ agents). Do not edit directly.
// Summary: Chip renders an inline span with optional decorative icon and accessible label override.

import type { ReactNode } from 'react';
import { collapseWhitespace, composeClasses } from '../../../utilities';

export interface ChipProps {
  label: string;
  ariaLabel?: string;
  icon?: ReactNode;
  className?: string;
}

const baseClasses = `inline-flex items-center gap-1 text-color-content-weak bg-background-none`;
const iconClasses = `shrink-0`;
const textClasses = `truncate`;

export const Chip = ({ label, ariaLabel, icon, className }: ChipProps) => {
  const ariaLabelValue = ariaLabel?.trim() ? ariaLabel : undefined;

  const baseClassName = collapseWhitespace(composeClasses(baseClasses, className));
  const iconClassName = collapseWhitespace(composeClasses(iconClasses));
  const textClassName = collapseWhitespace(composeClasses(textClasses));

  return (
    <span className={baseClassName} aria-label={ariaLabelValue}>
      {icon ? (
        <span aria-hidden="true" className={iconClassName}>
          {icon}
        </span>
      ) : null}
      <span className={textClassName}>{label}</span>
    </span>
  );
};
