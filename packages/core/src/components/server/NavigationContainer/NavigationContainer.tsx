// packages/core/src/components/server/NavigationContainer/NavigationContainer.tsx
// Generated by AI (DoXYZ agents). Do not edit directly.
// Summary: NavigationContainer wraps children in a div that applies navigation padding/background tokens and forwards HTML div props.

import * as React from 'react';
import { collapseWhitespace, composeClasses } from '../../../utilities';

export interface NavigationContainerProps extends React.HTMLAttributes<HTMLDivElement> {
  className?: string;
}

const baseClasses = `px-16 lg:px-112 py-24 lg:py-56 bg-background-none`;

export const NavigationContainer = (props: NavigationContainerProps) => {
  const { className, children, ...rest } = props;

  const rootClassName = collapseWhitespace(composeClasses(baseClasses, className));

  return (
    <div {...rest} className={rootClassName} data-slot="container">
      {children}
    </div>
  );
};

NavigationContainer.displayName = 'NavigationContainer';
