// packages/core/src/components/server/NavigationContainer/NavigationContainer.stories.tsx
// Generated by AI (DoXYZ agents). Do not edit directly.
// Summary: Storybook coverage for NavigationContainer showing default usage and composition within Footer.

import type { Meta, StoryObj } from '@storybook/react';
import { Markdown } from '@storybook/addon-docs/blocks';
import * as React from 'react';
import { Button } from '../../client/Button';
import { Footer as FooterWrapper } from '../Footer/Footer';
import { NavigationContainer, NavigationContainerProps } from './NavigationContainer';
import Readme from './README.md?raw';

const meta: Meta<NavigationContainerProps> = {
  title: 'Server components/NavigationContainer',
  component: NavigationContainer,
  tags: ['autodocs'],
  parameters: {
    docs: {
      page: () => <Markdown>{Readme}</Markdown>
    }
  },
  args: {
    children: (
      <div>
        <div>
          <Button variant="bare">Content and links go here</Button>
        </div>
        <div>
          <Button variant="bare">Content and links go here</Button>
        </div>
        <div>
          <Button variant="bare">Content and links go here</Button>
        </div>
      </div>
    )
  }
};

export default meta;

type Story = StoryObj<NavigationContainerProps>;

export const Default: Story = {
  args: {}
};

export const Footer: Story = {
  args: {
    children: (
      <div>
        <div>
          <Button variant="bare">Content and links go here</Button>
        </div>
        <div>
          <Button variant="bare">Content and links go here</Button>
        </div>
        <div>
          <Button variant="bare">Content and links go here</Button>
        </div>
      </div>
    )
  },
  render: args => (
    <FooterWrapper>
      <NavigationContainer {...args} />
    </FooterWrapper>
  )
};
