// packages/core/src/components/server/Badge/Badge.tsx
// Generated by AI (DoXYZ agents). Do not edit directly.
// Summary: Badge wraps Radix badge with gradient variants, optional icon slot, and label content.

import * as React from 'react';
import { Badge as RadixBadge } from '@radix-ui/themes';

const cx = (...classes: Array<string | undefined | null | false>) =>
  classes.filter(Boolean).join(' ');

export interface BadgeProps extends React.HTMLAttributes<HTMLSpanElement> {
  variant?: 'purple' | 'blue' | 'pink' | 'brand' | 'orange';
  icon?: React.ReactNode;
  label?: string;
}

const variantClasses: Record<NonNullable<BadgeProps['variant']>, string> = {
  purple: 'bg-gradient-purple',
  blue: 'bg-gradient-blue',
  pink: 'bg-gradient-pink',
  brand: 'bg-brand-500',
  orange: 'bg-gradient-orange'
};

export const Badge = React.forwardRef<HTMLSpanElement, BadgeProps>(
  ({ variant = 'purple', icon, label, className, ...props }, ref) => {
    return (
      <RadixBadge>
        <span
          ref={ref}
          className={cx(
            // root box
            'inline-flex items-center rounded-sm px-1.5 py-0.5',
            // typography
            'text-caption-lg-allcaps-bold',
            // variant
            variantClasses[variant],
            className
          )}
          {...props}
        >
          <span className="flex items-center gap-1.5">
            {icon && (
              <span className="shrink-0" aria-hidden="true">
                {icon}
              </span>
            )}
            {label && <span>{label}</span>}
          </span>
        </span>
      </RadixBadge>
    );
  }
);

Badge.displayName = 'Badge';
