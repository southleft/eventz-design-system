// packages/core/src/components/server/Text/Text.stories.tsx
// Generated by AI (Eventz agents). Do not edit directly.
// Summary: Storybook coverage for Text demonstrating default semantics plus size, weight, align, color, transform, italic, and truncate variants.

import type { Meta, StoryObj } from '@storybook/react-vite';
import { Markdown } from '@storybook/addon-docs/blocks';
import * as React from 'react';
import { Text, TextProps } from './Text';
import Readme from './README.md?raw';

const meta: Meta<TextProps> = {
  title: 'Server components/Text',
  component: Text,
  tags: ['autodocs'],
  parameters: {
    docs: {
      page: () => <Markdown>{Readme}</Markdown>
    }
  },
  args: {
    children: 'The quick brown fox jumps over the lazy dog.',
    size: 'base',
    weight: 'normal',
    align: 'inherit',
    color: 'default',
    transform: 'inherit',
    italic: false,
    truncate: false
  }
};

export default meta;

type Story = StoryObj<TextProps>;

const stackStyle: React.CSSProperties = {
  display: 'flex',
  flexDirection: 'column',
  gap: '0.75rem'
};

export const Default: Story = {
  args: {}
};

const sizeOptions: Array<Exclude<TextProps['size'], 'inherit'>> = [
  'xs',
  'sm',
  'base',
  'lg',
  'xl',
  '2xl',
  '3xl',
  '4xl',
  '5xl',
  '6xl',
  '7xl',
  '8xl'
];

export const Sizes: Story = {
  render: () => (
    <div style={stackStyle}>
      {sizeOptions.map((size) => (
        <Text key={size} size={size}>
          {`Size ${size}`}
        </Text>
      ))}
    </div>
  )
};

const weightOptions: Array<Exclude<TextProps['weight'], 'inherit'>> = [
  'thin',
  'extra-light',
  'light',
  'normal',
  'medium',
  'semi-bold',
  'bold',
  'extra-bold',
  'black'
];

export const Weights: Story = {
  render: () => (
    <div style={stackStyle}>
      {weightOptions.map((weight) => (
        <Text key={weight} weight={weight}>
          {`Weight ${weight}`}
        </Text>
      ))}
    </div>
  )
};

const alignOptions: Array<Exclude<TextProps['align'], 'inherit'>> = ['left', 'center', 'right', 'justify'];

export const Alignments: Story = {
  render: () => (
    <div style={{ ...stackStyle, width: '32rem' }}>
      {alignOptions.map((alignment) => (
        <Text key={alignment} align={alignment}>
          {`Alignment ${alignment}: Lorem ipsum dolor sit amet, consectetur adipiscing elit.`}
        </Text>
      ))}
    </div>
  )
};

const colorOptions: Array<Exclude<TextProps['color'], 'inherit'>> = [
  'default',
  'brand',
  'weak',
  'inverse',
  'subtle',
  'danger-strong',
  'danger-subtle',
  'warning-strong',
  'warning-subtle',
  'success-strong',
  'success-subtle',
  'info-strong',
  'info-subtle'
];

export const Colors: Story = {
  render: () => (
    <div style={stackStyle}>
      {colorOptions.map((color) => (
        <Text key={color} color={color}>
          {`Color ${color}`}
        </Text>
      ))}
    </div>
  )
};

const transformOptions: Array<Exclude<TextProps['transform'], 'inherit'>> = [
  'normal',
  'uppercase',
  'lowercase',
  'capitalize'
];

export const Transforms: Story = {
  render: () => (
    <div style={stackStyle}>
      <Text transform="inherit">Transform inherit (no class)</Text>
      {transformOptions.map((transform) => (
        <Text key={transform} transform={transform}>
          {`Transform ${transform} text`}
        </Text>
      ))}
    </div>
  )
};

export const Italic: Story = {
  args: {
    italic: true,
    children: 'Italic emphasis text.'
  }
};

export const Truncate: Story = {
  args: {
    truncate: true,
    children:
      'This is a very long line of text intended to demonstrate single-line truncation with an ellipsis when space is constrained.'
  },
  render: (args) => (
    <div style={{ width: '16rem' }}>
      <Text {...args} />
    </div>
  )
};
