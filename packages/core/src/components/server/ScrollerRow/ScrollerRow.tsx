// packages/core/src/components/server/ScrollerRow/ScrollerRow.tsx
// Generated by AI (DoXYZ agents). Do not edit directly.
// Summary: Flex row container for scroller content; forwards div attributes and merges className with layout tokens.

import * as React from 'react';
import { collapseWhitespace, composeClasses } from '../../../utilities';

const baseClasses = `
  flex
  gap-4
  justify-between
  items-center
`;

export interface ScrollerRowProps extends React.HTMLAttributes<HTMLDivElement> {
  className?: string;
}

export const ScrollerRow = React.forwardRef<HTMLDivElement, ScrollerRowProps>(
  ({ className, children, ...rest }, forwardedRef) => {
    const rootClassName = collapseWhitespace(composeClasses(baseClasses, className));

    return (
      <div {...rest} ref={forwardedRef} className={rootClassName}>
        {children}
      </div>
    );
  }
);

ScrollerRow.displayName = 'ScrollerRow';
