// packages/core/src/components/server/Footer/Footer.tsx
// Generated by AI (Eventz agents). Do not edit directly.
// Summary: Footer renders a div wrapper that applies the footer gradient class and forwards native div props.

import * as React from 'react';
import { collapseWhitespace, composeClasses } from '../../../utilities';

export interface FooterProps extends React.HTMLAttributes<HTMLDivElement> {
  className?: string;
}

const baseClasses = `background-footer`;

export const Footer = React.forwardRef<HTMLDivElement, FooterProps>((props, forwardedRef) => {
  const { className, children, ...rest } = props;

  const rootClassName = collapseWhitespace(composeClasses(baseClasses, className));

  return (
    <div {...rest} ref={forwardedRef} className={rootClassName}>
      {children}
    </div>
  );
});

Footer.displayName = 'Footer';
