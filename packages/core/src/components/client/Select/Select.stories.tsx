// packages/core/src/components/client/Select/Select.stories.tsx
// Generated by AI (DoXYZ agents). Do not edit directly.
// Summary: Stories for Select showcasing default, info-rich, disabled, error, and long-list configurations using custom renders and optional CircleCheckIcon adornments.

import { Markdown } from '@storybook/addon-docs/blocks';
import * as React from 'react';
import type { Meta, StoryObj } from '@storybook/react';
import { Select, type SelectProps } from './Select';
import { CircleCheckIcon } from '../../../icons';
import Readme from './README.md?raw';

const meta: Meta<SelectProps> = {
  title: 'Client components/Select',
  component: Select,
  args: {
    disabled: false
  },
  tags: ['autodocs'],
  parameters: {
    docs: {
      page: () => <Markdown>{Readme}</Markdown>
    }
  }
};

export default meta;

type Story = StoryObj<SelectProps>;

export const Default: Story = {
  args: {
    InputProps: {
      placeholder: 'Select an option'
    }
  },
  render: args => (
    <Select {...args} options={[{ option: 'Alpha' }, { option: 'Bravo' }, { option: 'Charlie' }]} />
  )
};

export const Complicated: Story = {
  args: {
    InputProps: {
      label: 'Choices',
      placeholder: 'Select an option',
      info: 'Select an option from the phonetic alphabet.',
      hint: 'Phonetic alphabet options'
    }
  },
  render: args => (
    <Select
      {...args}
      options={[
        { option: 'Alpha', startIcon: <CircleCheckIcon /> },
        { option: 'Bravo', startIcon: <CircleCheckIcon /> },
        { option: 'Charlie', startIcon: <CircleCheckIcon /> }
      ]}
    />
  )
};

export const Disabled: Story = {
  args: {
    disabled: true,
    InputProps: {
      placeholder: 'Disabled select'
    }
  },
  render: args => (
    <Select {...args} options={[{ option: 'Alpha' }, { option: 'Bravo' }, { option: 'Charlie' }]} />
  )
};

export const ErrorState: Story = {
  args: {
    InputProps: {
      placeholder: 'Pick one',
      hint: 'Example error',
      error: 'Selection required'
    }
  },
  render: args => (
    <Select
      {...args}
      options={[{ option: 'Pending' }, { option: 'Approved' }, { option: 'Denied' }]}
    />
  )
};

export const LongList: Story = {
  args: {
    InputProps: {
      placeholder: 'Scroll to find an option'
    }
  },
  render: args => (
    <Select
      {...args}
      options={Array.from({ length: 50 }, (_, index) => ({
        option: `Option ${index + 1}`
      }))}
    />
  )
};
