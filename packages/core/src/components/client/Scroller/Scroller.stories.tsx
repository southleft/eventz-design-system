// packages/core/src/components/client/Scroller/Scroller.stories.tsx
// Generated by AI (DoXYZ agents). Do not edit directly.
// Summary: Stories for Scroller covering default paging, fixed step pagination, stacked row layout, and control-free usage with Next.js composition note.

import type { Meta, StoryObj } from '@storybook/react';
import * as React from 'react';
import { Scroller, type ScrollerProps } from './Scroller';

const cardClasses = `
  flex h-40 min-w-[16rem] items-center justify-center rounded-lg bg-color-background-brand-subtle text-color-content-strong font-medium
`;

const spacedCardClasses = `
  ${cardClasses} mr-4 last:mr-0
`;

const defaultChildren = Array.from({ length: 10 }, (_, index) => (
  <div key={index} className={spacedCardClasses}>
    Item {index + 1}
  </div>
));

const meta: Meta<ScrollerProps> = {
  title: 'Client components/Scroller',
  component: Scroller,
  args: {
    showControls: true,
    pageBy: 'viewport',
    children: defaultChildren
  },
  parameters: {
    docs: {
      description: {
        component:
          'Manual horizontal scroller with prev/next Controls. Next.js note: pass server-rendered children (e.g., `<ScrollerRow>` or `<MediaCard>`) from a parent server component; do not import server components into this client Scroller.'
      }
    }
  }
};

export default meta;

type Story = StoryObj<ScrollerProps>;

export const Default: Story = {};

export const FixedPage: Story = {
  args: {
    pageBy: 'fixed',
    pageSize: 320
  }
};

const stackedRows = (
  <>
    {Array.from({ length: 2 }, (_, rowIndex) => (
      <div key={rowIndex} className="flex gap-4 mb-4 last:mb-0">
        {Array.from({ length: 4 }, (_, itemIndex) => (
          <div key={itemIndex} className="flex h-32 min-w-[14rem] items-center justify-center rounded-lg bg-color-background-accent-subtle text-color-content-strong font-medium">
            Row {rowIndex + 1}, Item {itemIndex + 1}
          </div>
        ))}
      </div>
    ))}
  </>
);

export const StackRows: Story = {
  args: {
    stackRows: true,
    children: stackedRows
  }
};

export const NoControls: Story = {
  args: {
    showControls: false
  }
};

