// This file was generated by an AI agent. Do not edit directly.
import * as React from 'react';
import type { Meta, StoryObj } from '@storybook/react';
import { NavigationDropdown } from '../NavigationDropdown';
import { NavigationBar, NavigationBarProps } from './NavigationBar';

const baseItems: NavigationBarProps['items'] = [
  { label: 'Overview', href: '/overview' },
  { label: 'Solutions', href: '/solutions' },
  { label: 'Pricing', href: '/pricing' },
  { label: 'Docs', href: '/docs' }
];

const defaultLogo = <span className="text-lg font-bold text-color-content-default">DoXYZ</span>;

const defaultMobileNavigation = (
  <button type="button" aria-label="Open navigation menu" data-state="closed">
    ☰
  </button>
);

const meta: Meta<NavigationBarProps> = {
  title: 'Components/NavigationBar',
  component: NavigationBar,
  args: {
    ariaLabel: 'Primary navigation',
    items: baseItems,
    logo: defaultLogo,
    mobileNavigation: defaultMobileNavigation
  }
};

export default meta;

type Story = StoryObj<NavigationBarProps>;

export const Default: Story = {};

export const Fixed: Story = {
  args: {
    fixed: true
  }
};

export const CurrentItem: Story = {
  args: {
    items: [
      { label: 'Overview', href: '/overview' },
      { label: 'Solutions', href: '/solutions', current: true },
      { label: 'Pricing', href: '/pricing' },
      { label: 'Docs', href: '/docs' }
    ]
  }
};

export const Minimal: Story = {
  args: {
    logo: undefined,
    mobileNavigation: undefined,
    secondaryNavigation: undefined,
    items: [
      { label: 'Home', href: '/' },
      { label: 'Features', href: '/features' }
    ]
  }
};

export const RichSecondary: Story = {
  args: {
    secondaryNavigation: (
      <div className="flex items-center gap-4">
        <input
          type="search"
          placeholder="Search"
          className="h-40 rounded-sm border border-color-border-default px-4"
        />
        <div className="flex items-center gap-3">
          <button type="button" className="text-sm font-medium text-color-content-default">
            Sign in
          </button>
          <button
            type="button"
            className="rounded-md bg-comp-button-primary-color-background-default px-5 py-2 text-sm font-semibold text-comp-button-primary-color-content-default"
          >
            Get Started
          </button>
        </div>
      </div>
    )
  }
};

export const WithDropdown: Story = {
  args: {
    mobileNavigation: (
      <NavigationDropdown
        items={[
          { label: 'Overview', href: '/overview' },
          { label: 'Solutions', href: '/solutions' },
          { label: 'Pricing', href: '/pricing' }
        ]}
      />
    )
  }
};
