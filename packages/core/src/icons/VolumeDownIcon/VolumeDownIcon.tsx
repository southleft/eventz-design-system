// packages/core/src/icons/VolumeDownIcon/VolumeDownIcon.tsx
// Generated by AI (DoXYZ agents). Do not edit directly.
// Summary: VolumeDownIcon renders a volume-down glyph SVG supporting decorative or labeled usage with optional title and color props

import * as React from 'react';

export interface VolumeDownIconProps extends React.SVGAttributes<SVGSVGElement> {
  decorative?: boolean;
  title?: string;
  titleId?: string;
  color?: string;
}

type VolumeDownIconElement = SVGSVGElement;

export const VolumeDownIcon = React.forwardRef<VolumeDownIconElement, VolumeDownIconProps>(
  (
    {
      decorative = true,
      width = '20px',
      height = '20px',
      title,
      titleId: providedTitleId,
      color = 'currentColor',
      className,
      'aria-labelledby': ariaLabelledby,
      ...rest
    },
    ref
  ) => {
    const generatedTitleId = React.useId();
    const resolvedTitleId = title ? (providedTitleId ?? generatedTitleId) : undefined;

    const computedAriaHidden = decorative ? 'true' : undefined;
    const computedRole = decorative ? undefined : 'img';
    const computedAriaLabelledby =
      !decorative && title && resolvedTitleId ? resolvedTitleId : ariaLabelledby;

    return (
      <svg
        ref={ref}
        {...rest}
        className={className}
        viewBox="0 0 20 20"
        width={width}
        height={height}
        fill={color}
        aria-hidden={computedAriaHidden}
        role={computedRole}
        aria-labelledby={computedAriaLabelledby}
      >
        {!decorative && title ? <title id={resolvedTitleId}>{title}</title> : null}
        <g transform="scale(0.0208333333,0.0208333333) translate(0,960)">
          <path d="M230.2-375.87v-208.26h148.78l203.24-203.24v614.74L378.98-375.87H230.2Zm424.02 49.74v-307.74q47.67 19.96 73.63 62.67 25.95 42.72 25.95 91.2 0 48.48-25.95 90.7-25.96 42.21-73.63 63.17Zm-155-260.94-85.94 85.94H313.2v42.26h100.08l85.94 85.94v-214.14ZM405.09-480Z" />
        </g>
      </svg>
    );
  }
);

VolumeDownIcon.displayName = 'VolumeDownIcon';
