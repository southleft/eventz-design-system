// packages/core/src/icons/RemoveIcon/RemoveIcon.tsx
// Generated by AI (DoXYZ agents). Do not edit directly.
// Summary: RemoveIcon renders a remove glyph SVG supporting decorative or labeled usage with optional title and color props

import * as React from 'react';

export interface RemoveIconProps extends React.SVGAttributes<SVGSVGElement> {
  decorative?: boolean;
  title?: string;
  titleId?: string;
  color?: string;
}

type RemoveIconElement = SVGSVGElement;

export const RemoveIcon = React.forwardRef<RemoveIconElement, RemoveIconProps>(
  (
    {
      decorative = true,
      width = '20px',
      height = '20px',
      title,
      titleId: providedTitleId,
      color = 'currentColor',
      className,
      'aria-labelledby': ariaLabelledby,
      ...rest
    },
    ref
  ) => {
    const generatedTitleId = React.useId();
    const resolvedTitleId = title ? (providedTitleId ?? generatedTitleId) : undefined;

    const computedAriaHidden = decorative ? 'true' : undefined;
    const computedRole = decorative ? undefined : 'img';
    const computedAriaLabelledby =
      !decorative && title && resolvedTitleId ? resolvedTitleId : ariaLabelledby;

    return (
      <svg
        ref={ref}
        {...rest}
        className={className}
        viewBox="0 0 20 20"
        width={width}
        height={height}
        fill={color}
        aria-hidden={computedAriaHidden}
        role={computedRole}
        aria-labelledby={computedAriaLabelledby}
      >
        {!decorative && title ? <title id={resolvedTitleId}>{title}</title> : null}
        <g transform="scale(0.0208333333,0.0208333333) translate(0,960)">
          <path d="M223.87-438.5v-83h512.26v83H223.87Z" />
        </g>
      </svg>
    );
  }
);

RemoveIcon.displayName = 'RemoveIcon';
