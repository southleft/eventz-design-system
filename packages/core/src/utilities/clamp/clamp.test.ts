// packages/core/src/utilities/clamp/clamp.test.ts
// Generated by AI (DoXYZ agents). Do not edit directly.
// Summary: Validates clamp handles NaN input, inverted ranges, and boundary enforcement.

import { clamp } from './clamp';

describe('clamp', () => {
  it('returns min when value is NaN', () => {
    expect(clamp(Number.NaN, 0, 10)).toBe(0);
  });

  it('returns min when max is below min', () => {
    expect(clamp(5, 0, -1)).toBe(0);
  });

  it('caps values above max', () => {
    expect(clamp(15, 0, 10)).toBe(10);
  });

  it('caps values below min', () => {
    expect(clamp(-2, 0, 10)).toBe(0);
  });

  it('returns value when within range', () => {
    expect(clamp(5, 0, 10)).toBe(5);
  });
});
