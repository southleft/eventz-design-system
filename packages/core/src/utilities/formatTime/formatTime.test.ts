// packages/core/src/utilities/formatTime/formatTime.test.ts
// Generated by AI (DoXYZ agents). Do not edit directly.
// Summary: Ensures formatTime zero-pads minutes/seconds and guards invalid inputs.

import { formatTime } from './formatTime';

describe('formatTime', () => {
  it('returns 00:00 for negative values', () => {
    expect(formatTime(-1)).toBe('00:00');
  });

  it('returns 00:00 for NaN', () => {
    expect(formatTime(Number.NaN)).toBe('00:00');
  });

  it('formats whole minutes and seconds', () => {
    expect(formatTime(75)).toBe('01:15');
  });

  it('floors fractional seconds', () => {
    expect(formatTime(125.9)).toBe('02:05');
  });
});
