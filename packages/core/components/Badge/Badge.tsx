// This file was generated by an AI agent. Do not edit directly.
import * as React from 'react';
import { Badge as RadixBadge } from 'radix-ui';
import { Slot } from '@radix-ui/react-slot';

const cx = (...classes: Array<string | undefined | null | false>) =>
  classes.filter(Boolean).join(' ');

export interface BadgeProps extends React.HTMLAttributes<HTMLSpanElement> {
  variant?: 'purple' | 'blue' | 'pink' | 'brand' | 'orange';
  icon?: React.ReactNode;
  label?: string;
  asChild?: boolean;
}

export const Badge = React.forwardRef<HTMLSpanElement, BadgeProps>(
  (
    {
      variant = 'purple',
      icon,
      label,
      asChild = false,
      className,
      ...props
    },
    ref
  ) => {
    const Component = asChild ? Slot : 'span';
    const variantClasses: Record<Required<BadgeProps>['variant'], string> = {
      purple: 'bg-purple-500 text-white',
      blue: 'bg-blue-500 text-white',
      pink: 'bg-pink-500 text-white',
      brand: 'bg-brand-500 text-white',
      orange: 'bg-orange-500 text-white'
    };

    return (
      <RadixBadge asChild={asChild}>
        <Component
          ref={ref}
          className={cx(
            'inline-flex items-center rounded-full text-label-sm font-medium px-2.5 py-1',
            'flex items-center gap-1.5',
            variantClasses[variant],
            className
          )}
          {...props}
        >
          {icon && (
            <span className="shrink-0" aria-hidden="true">
              {icon}
            </span>
          )}
          {label && <span>{label}</span>}
        </Component>
      </RadixBadge>
    );
  }
);

Badge.displayName = 'Badge';

