# ──────────────────────────────────────────────────────────
# Company Docs MCP — Environment Configuration
# ──────────────────────────────────────────────────────────
# Copy this file to .env and fill in your values.
# Never commit .env to version control.

# ── Required ─────────────────────────────────────────────

# Supabase — stores your documentation and powers search
SUPABASE_URL="https://your-project.supabase.co"
SUPABASE_ANON_KEY="your-anon-key"
SUPABASE_SERVICE_KEY="your-service-role-key"

# Cloudflare — Account ID for AI embedding generation
# Find this at: dash.cloudflare.com (right sidebar on overview page)
CLOUDFLARE_ACCOUNT_ID="your-account-id"

# ── Optional ─────────────────────────────────────────────

# Organization branding (used by the chat UI)
# ORGANIZATION_NAME="Your Company Name"
# ORGANIZATION_LOGO_URL=""
# ORGANIZATION_TAGLINE="Get instant answers from our documentation."

# OpenAI — only needed if using OpenAI embeddings or the AI chat feature
# Search, MCP tools, and Slack all work without this.
# OPENAI_API_KEY="sk-..."

# Embedding provider override
# Default: workers-ai (auto-detected). Set to "openai" if you prefer OpenAI embeddings.
# If OPENAI_API_KEY is set without this, it auto-selects OpenAI for backward compatibility.
# EMBEDDING_PROVIDER="workers-ai"

# Slack integration (optional — see docs/SLACK_SETUP.md)
# SLACK_SIGNING_SECRET="your-signing-secret"
# SLACK_SLASH_COMMAND="/docs"
