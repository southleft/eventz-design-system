#!/usr/bin/env tsx
/**
 * Alias for ingest-supabase.ts — forwards all arguments.
 *
 * Usage: npm run ingest:supabase -- [options]
 * See ingest-supabase.ts for full documentation.
 */

console.log("This script has been consolidated into ingest-supabase.ts.");
console.log("Run: npm run ingest:supabase -- [options]");
console.log("  --clear      Wipe and re-ingest everything");
console.log("  --dry-run    Preview without writing");
console.log("  --verbose    Detailed output");
process.exit(0);
